
# Setup Guide

## Prerequisites
- Git installed
- GitHub account

## Steps
1. Clone the repo
2. Checkout `dev` branch for latest development work
3. Create feature branches from `dev`


git commit -m "docs: add setup guide"
git push -u origin feature/add-docs